# Azure KB Ingestion — Environment Variables
# ============================================
# Populate from AZD outputs after provisioning:
#   azd env get-values > src/functions/.env
#
# Or copy this file and fill in manually:
#   cp .env.sample .env

# --- Azure AI Services (Content Understanding + Embeddings) ---
AI_SERVICES_ENDPOINT=https://<ai-services-name>.cognitiveservices.azure.com/

# --- Embedding Model ---
EMBEDDING_DEPLOYMENT_NAME=text-embedding-3-small

# --- Azure AI Search ---
SEARCH_ENDPOINT=https://<search-service-name>.search.windows.net
SEARCH_INDEX_NAME=kb-articles

# --- Authentication ---
# No keys needed — uses DefaultAzureCredential (az login for local dev,
# managed identity when deployed). Ensure your identity has the required
# RBAC roles (see Epic 001, Story 2).
